# WebSocket <-> TCP Proxy

We need to convert ws packets into TCP and vice verse. So I decided to use Bun for that. But you can use whatever you want.

You can adjust `PORT` and `HOSTNAME` env variables for listening ws connections.
